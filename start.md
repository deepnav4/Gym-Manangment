# Gym Management System - Credentials & Start Guide

Since the backend API is currently offline, a full-fidelity client-only mock fallback has been integrated into the frontend. You can sign up with any email or log in instantly using the pre-configured credentials below.

## Demo Login Credentials

### 1. Member Dashboard
* **Email**: `member@gym.com`
* **Password**: `gympass123`

### 2. Trainer / Coach Dashboard
* **Email**: `trainer@gym.com`
* **Password**: `gympass123`

---

## Features Supported in Mock Mode
* **Session Persistence**: Login sessions and user details are remembered between page reloads using `localStorage`.
* **Stateful Actions**: Adding workouts, diet plans, recording attendance, and registering body progress as a trainer will save to local lists and immediately reflect inside the member dashboard!
